# Product data

Place `products_list.csv` in this folder before running `pnpm run seed`.

Expected columns: `displayTitle, embeddingText, url, imageUrl, productType, discount, price, variants, createDate`.

The file itself is gitignored (see root `.gitignore`) since it's private data.
